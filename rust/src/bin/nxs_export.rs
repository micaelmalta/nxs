//! `nxs-export` binary entry point. Stub. See plan step `impl: nxs-export CLI`.

fn main() {
    eprintln!("nxs-export: not yet implemented — see context/plans/2026-04-30-converter-suite.md");
    std::process::exit(2);
}
