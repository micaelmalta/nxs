//! `nxs-import` binary entry point. Stub — flag parsing + dispatch to
//! `nxs::convert::run_import`. See plan step `impl: nxs-import CLI`.

fn main() {
    eprintln!("nxs-import: not yet implemented — see context/plans/2026-04-30-converter-suite.md");
    std::process::exit(2);
}
