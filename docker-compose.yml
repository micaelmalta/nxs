services:
  nxs:
    build:
      context: ./js
      dockerfile: Dockerfile
    volumes:
      - ./js:/app
      - ./js/fixtures:/app/fixtures
    restart: always
    networks:
      - shared_network

networks:
  shared_network:
    external: true
